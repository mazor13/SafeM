{
  "name": "safem-functions",
  "version": "1.0.0",
  "description": "SafeM Cloud Functions for Firebase",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "lint": "echo 'Lint placeholder - add eslint if needed'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0",
    "pdf-lib": "^1.17.1",
    "@google-cloud/kms": "^3.8.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
